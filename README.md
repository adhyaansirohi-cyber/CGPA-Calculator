#CGPA Calculator - 
 A simple Login and Registration System built in C++ using file handling. Users can register with a username and password, and the credentials are stored in a text file (users.txt). The system allows users to log in by verifying stored data.
